/**
 * TODO Manage a sound queue
 */

import React from 'react';
import { assert, assMatch } from 'sjtest';
import {getType} from '../base/data/DataClass';
import GameControls from '../GameControls';
import Sound from 'react-sound';

class SoundQ extends React.Component {
    // render() {
	// 	if (this.refs.canvas) {
	// 		this.updateCanvas();
	// 	}
	// 	// NB if the width is set in css -- weird zooming occurs!
    //     return (
	// 		<div ref="cwrapper" >
	// 			<canvas width={this.props.width} height={this.props.height} ref="canvas" />
	// 		</div>
    //     );
    // }
}

export default SoundQ;
